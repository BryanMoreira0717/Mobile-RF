const colors = {
  // Marca / Primária
  primary: "#10b981",       // verde principal (botões, ícones, destaques)
  primaryDark: "#064e3b",   // verde escuro (logo, títulos, textos de marca)
  primaryDeep: "#006c49",   // verde usado em valores de destaque (ex: total)
  primaryLight: "#ECFDF5",  // verde bem claro (fundo de badges, hero section)

  // Acento
  accent: "#d97706",        // laranja/âmbar (labels, valores em kg, "Franca")

  // Neutros / Texto
  background: "#fbfbf9",    // fundo geral das telas
  white: "#ffffff",         // cards, headers, bottom nav
  textPrimary: "#1f2937",   // texto principal (títulos, valores)
  textSecondary: "#4b5563", // texto secundário (labels, descrições)
  textTertiary: "#c2c7d1",  // texto terciário / placeholder
  textLight: "#e5e5e5",     // texto sobre áreas de upload/placeholder

  // Bordas / Divisores
  border: "#d1d5db",        // borda padrão de inputs e cards
  borderLight: "#e5e7eb",   // divisores (headers, progress bar bg)
  borderMuted: "#bbcabf",   // borda em cards de pagamento

  // Extras (telas de pagamento)
  textMuted: "#3c4a42",     // texto de cabeçalho em card de pagamento
  textDark: "#121c2a",      // texto "Total" em tela de checkout
}

export default colors;